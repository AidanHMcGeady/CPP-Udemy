#include <iostream>

using namespace std;

main()
{
    char chr = a; //Char (or Character) is a variable that contains a single alphabetic character
    int nt = 1; //Int (or Integer) is a variable that contains a whole number value
    short shrt = 12345; //Short (or Short Integer) is a variable like an integer but has a maximum length it can go up to
    float flt = 0.3; //Float is a variable that can have any amount of numbers after its decimal place
    double dbl = 0.46; //Double is a variable that can only have 2 numbers after its decimal place
}
